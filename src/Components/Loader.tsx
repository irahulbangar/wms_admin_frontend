const Loader = () => {
  return (
    <div className="flex items-center justify-center h-full fixed inset-0 z-50 bg-black/50 bg-opacity-40">
      <span className="loader"></span>
    </div>
  );
};

export default Loader;
